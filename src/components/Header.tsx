import React from 'react'
import { Appbar } from 'react-native-paper'

const Header = ({

}) => (
  <Appbar.Header>
    <Appbar.Content title="My App" style={{ alignItems: 'center' }} />
  </Appbar.Header>
)

export default Header
